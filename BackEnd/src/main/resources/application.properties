spring.datasource.url= jdbc:mysql://localhost:3306/easycarrent?createDatabaseIfNotExist=true
spring.datasource.username= root
spring.datasource.password= Rajith784349@

entity.package.name=app.entity
my.app.driverClassName=com.mysql.cj.jdbc.Driver

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto = update

## Hibernate Logging
logging.level.org.hibernate.SQL= DEBUG

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.nicImage-size-threshold=2KB
# Max nicImage size.
spring.servlet.multipart.max-nicImage-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

